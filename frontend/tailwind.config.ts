import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#e72e77",
      },
    },
  },
  plugins: [],
};

export default config;
